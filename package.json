{
  "name": "game-review-platform",
  "version": "1.0.0",
  "description": "Game Review Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run build && npm start --prefix backend",
    "dev": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "author": "Game Review Team",
  "license": "ISC"
}
